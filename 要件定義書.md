# 競馬予想支援ツール 要件定義書

パドックAI解析 × 定量ファクター統合型（個人利用・非商用）

| 項目 | 内容 |
|---|---|
| 版数 | v1.1 |
| 作成日 | 2026-08-07 |
| 更新日 | 2026-08-07（5.8運用フロー追加、12章にDB永続化ギャップ追記、14章関連ドキュメント追加） |
| 対象フェーズ | Phase 1（MVP） |
| ステータス | レビュー中（未承認） |

> 本書は `CLAUDE.md`（実装時にAIが参照する仕様書）のPhase 1部分を、これまでの協議内容に基づき要件定義書として整理し直したものである。コード実装を再開する前に、本書の内容についてレビュー・確定を行う。

---

## 1. 目的・背景

競馬の予想精度向上を目的として、パドックにおける馬体・状態評価を中核ファクターの一つとしつつ、オッズ・過去成績等の定量データと組み合わせたハイブリッド型の予想支援ツールを開発する。

パドック動画のAI解析（姿勢推定による歩様特徴抽出）は、先行事例（JRA関連企業の取り組みや個人開発者の事例）で以下の課題が指摘されている。

- カメラ角度や馬の向きによる解析ノイズの大きさ
- 十分な精度を得るために必要な学習データ量（先行事例では約1,000頭規模）
- 個人開発レベルでの精度の限界

これを踏まえ、開発をフェーズ分けし、まず**定量ファクター＋手動パドック評価のみで予想・バックテストが機能するMVP（Phase 1）**を構築する。パドック動画のAI解析（M2/M3）はPhase 2以降の試験導入とし、**本書ではPhase 1のみを要件定義の対象とする**。

## 2. 用語定義

| 用語 | 説明 |
|---|---|
| パドック | レース前に出走馬を周回させ、馬体・気配・歩様等を関係者や観客が確認するエリア。 |
| JV-Link / JV-Data | JRA-VANが提供するデータ取得用ソフトウェア（ActiveX/COM）およびそれを通じて取得できるレース関連データ。 |
| DataLab.（データラボ） | JRA-VANが提供する競馬データ会員サービス。月額課金制で、JV-Data等の基礎データを利用できる。 |
| スコアリングエンジン | 各ファクターの値を統合し、出走馬ごとの評価値（スコア）を算出する処理系。 |
| バックテスト | 過去のレース結果に予想ロジックを適用し、的中率・回収率等の成績をシミュレーションする検証手法。 |
| 回収率（ROI） | 投資額に対する回収額の割合。100%を超えると期待値上プラスであることを示す。 |
| クラス昇降 | 出走馬の近走クラスと、今回出走するレースのクラスとの差。上のクラスに挑戦するほど不利、下のクラスに出走するほど有利と評価する。 |
| 近走成績（スピード指数近似） | 直近数走の着順・上がり3ハロンから算出する、その馬の現在の調子・能力の簡易的な指標。 |

## 3. スコープ

### 3.1 対象範囲（Phase 1）

- 定量ファクター（オッズ・馬体重・騎手／調教師成績・過去成績由来のクラス昇降と近走成績）によるスコアリング
- 手動パドック評価（M4）の入力とスコアリングへの統合
- ルールベースのスコアリングエンジンと、その重み付けを検証・探索する仕組み
- 過去データを用いたバックテスト（的中率・回収率の検証）
- ランキング表示・買い目候補の提示（CLI/CSV、将来的にStreamlit等のUI）

### 3.2 対象外（本書のスコープ外）

| 項目 | 扱い |
|---|---|
| M2: パドック動画取得・前処理 | Phase 2以降。ユーザーからの明示的な指示があるまで着手しない。 |
| M3: 姿勢推定・歩様特徴抽出（AI） | 同上。 |
| 血統評価の定量化 | JV-Dataは父・母・母父の「名前」のみを提供し、産駒成績等の強さを示すデータを含まないため、外部データベースを別途組み込むまでPhase 1では対象外とする。 |
| 買い目の資金配分（金額配分ロジック） | 本ツールは券種別の推奨銘柄（馬番）の提示までを行い、賭け金の配分計算は対象外とする。 |
| 機械学習モデルへの移行 | Phase 3で検討。Phase 1はルールベーススコアのみ。 |

## 4. システム全体構成

| ID | モジュール名 | Phase 1での状態 |
|---|---|---|
| M1 | 基礎データ収集モジュール | 対象。ただし実データ接続（JV-Link）は未着手、モックデータで代替（6章参照） |
| M2 | パドック動画取得・前処理モジュール | 対象外（Phase 2） |
| M3 | 姿勢推定・歩様特徴抽出モジュール（AI） | 対象外（Phase 2） |
| M4 | 手動パドック評価入力モジュール | 対象 |
| M5 | 特徴量統合・前処理モジュール | 対象 |
| M6 | スコアリング・予測モデルモジュール | 対象（ルールベースのみ） |
| M7 | 出力・UIモジュール | 対象（CLI/CSV。Streamlit等は未着手） |
| M8 | バックテスト・検証モジュール | 対象。ただし実データ未入手のため合成データでの動作確認のみ |
| M9 | 重み探索モジュール | 対象。M6の重みをバックテスト結果から探索する補助機能 |

## 5. 機能要件

### 5.1 M1 基礎データ収集モジュール

- **入力**: JRA-VAN利用キー、対象レースID・開催日
- **処理**: JV-Link経由でJV-Data（番組表・出走馬・血統・オッズ・馬体重・過去成績・天候／馬場状態）を取得し、ローカルDBに保存・更新する。
- **出力**: レース単位・出走馬単位の基礎データテーブル
- **現状**: JRA-VAN契約・JV-Link利用キーが未取得のため、実データ接続は未実装。開発・動作確認は架空データを返すモックデータソースで代替している（8章参照）。

### 5.2 M4 手動パドック評価入力モジュール

- **入力**: 利用者本人の目視によるパドック観察結果
- **処理**: 以下の項目を-2（悪い）〜+2（良い）の5段階で採点し、馬ごとの評価スコア（合計値）に変換する。
  - **状態系**（レースごとに変動する当日の状態）: 毛艶、発汗、歩くリズム
  - **馬体系**（比較的変化の少ない体型的特徴）: 脚の長さ、胴の長さ、トモの筋肉量、胸前の筋肉
- **出力**: 馬ごとの手動パドック評価スコア（合計値、正規化した値）
- **未決定事項**: 各項目の「+2」「-2」等の具体的な判断基準（採点基準表）はまだ言語化されていない（12.1参照）。

### 5.3 M5 特徴量統合・前処理モジュール

- **入力**: M1（基礎データ）、M4（手動評価スコア）
- **処理**: レース・馬番単位でデータを結合し、以下の特徴量を算出する。
  - オッズの実勢確率（過剰配当分を除去した正規化済み勝率相当）
  - 前走からの馬体重変化率
  - 騎手・調教師の成績
  - 手動パドック評価スコアの正規化値
  - クラス昇降（直近3走の平均クラス と 今回のクラスとの差）
  - 近走成績（直近3走の着順・上がり3ハロンから算出する簡易指標）
- **出力**: 出走馬ごとの統合特徴量テーブル
- **欠損値処理**: 手動パドック評価が未入力の馬・過去成績が存在しない馬（デビュー戦等）は、該当特徴量を中立値として扱う。

### 5.4 M6 スコアリング・予測モデルモジュール

- **入力**: M5の統合特徴量テーブル
- **処理**: 各特徴量をレース内でMin-Max正規化した上で、以下の重みで加重合計しスコア化する（0〜100点）。

  | ファクター | 重み | 出典 |
  |---|---|---|
  | 手動パドック評価（M4） | 0.35 | 手動入力 |
  | オッズ（実勢確率） | 0.25 | JV-Data |
  | 近走成績 | 0.12 | JV-Data（過去成績） |
  | クラス昇降 | 0.10 | JV-Data（過去成績） |
  | 騎手成績 | 0.08 | JV-Data |
  | 馬体重の安定度 | 0.05 | JV-Data |
  | 調教師成績 | 0.05 | JV-Data |

- **出力**: 出走馬ごとのスコア、順位、ファクター別の内訳
- **未決定事項**: 上記の重みは暫定値であり、実過去データによるバックテストでの妥当性検証（M9）が必要（12.2参照）。

### 5.5 M7 出力・UIモジュール

- **入力**: M6のスコア・順位・内訳
- **処理**: 出走馬をスコア順にランキング表示し、以下の買い目候補を提示する。
  - 単勝: スコア1位のみ
  - 複勝: スコア上位2頭
  - 馬連: スコア上位3頭のボックス
- **出力**: ランキング表示（CLI）、CSV出力、各馬のファクター別内訳
- **対象外**: 賭け金の配分計算は行わない（3.2参照）。
- **未着手**: Streamlit等によるブラウザ上での入出力（現状はCLI/CSVのみ）。

### 5.6 M8 バックテスト・検証モジュール

- **入力**: 過去レースの統合特徴量、実際の着順、確定オッズ
- **処理**: M6のスコアリングロジックを過去データに適用し、単勝・複勝それぞれの的中率・回収率（ROI）を集計する。
- **出力**: バックテスト結果レポート（対象レース数、券種別の的中率・回収率）
- **現状**: 実過去データが未入手のため、パイプラインの動作確認は合成（架空）データで行っている。**実データ入手までは、算出される的中率・回収率の数値に予測的な意味はない**（12.2参照）。

### 5.7 M9 重み探索モジュール

- **入力**: バックテスト用データ（特徴量・結果・オッズ）
- **処理**: M6の重み（7ファクターの配分）をランダムサーチで探索し、単勝回収率を主指標に評価する。現行の重みと探索結果を比較表示する。
- **出力**: 重み候補ごとのバックテスト成績（回収率・的中率）ランキング
- **現状**: 合成データ上での探索の仕組みとしては動作確認済み。実際に意味のある重み調整には実過去データが必要。

### 5.8 運用フロー（週次サイクル）

M1〜M9は個別の機能要件として定義したが、実際の利用イメージとして、週末のレース開催に合わせた運用サイクルを以下に定義する。図（Mermaidシーケンス図）は `docs/05_処理フロー.md` UC0 に記載する。

| タイミング | 作業内容 | 対応モジュール | 現状の制約 |
|---|---|---|---|
| 開催前（金曜等） | 対象レースの番組表・出走馬情報・オッズ等を取得 | M1 | 実データ未接続のため、現状はJRA-VAN公式サイト等で手動確認する運用となる |
| 開催当日・レース前（現地） | パドックで馬体・状態を観察し、採点基準に沿って評価する | M4 | CLIへの入力経路が未接続（`prompt_score_cli`が未接続、要件定義書12.1項目3の採点基準具体化も未完了）のため、現状は手元でメモを取るに留まる |
| レース発走前 | `python -m keiba.cli run` を実行し、ランキング・買い目候補を確認する | M5, M6, M7 | 現状はM4評価を反映できず、定量ファクターのみでのスコアリングとなる |
| レース確定後 | 着順・払戻を記録する | （M8の入力データ） | DBが未接続のため、現状は記録が自動的には蓄積されない（要件定義書12.2項目6） |
| 開催終了後・週次 | 蓄積した結果でバックテストを更新し、重み（M6）の妥当性を`tune-weights`で検証する | M8, M9 | 実データが無いため、現状は`backtest-demo`/`tune-weights`による合成データでの動作確認に留まる |

**現状のまとめ**: 上記サイクルは「あるべき姿」として設計したものであり、M1（実データ接続）・M4（CLI接続）・DB永続化という3つの既知ギャップが解消されるまでは、実際にはM7のランキング表示のみを都度単発で利用する運用にとどまる。

## 6. データ要件

### 6.1 入力データ項目

| カテゴリ | 主な項目 | 出典 |
|---|---|---|
| 出走馬情報 | 馬名、年齢、性別、斤量、騎手、調教師、血統（父・母・母父）、所属 | JV-Data |
| 過去成績 | 着順、タイム、上がり3ハロン、クラス、距離、馬場状態 | JV-Data |
| オッズ | 単勝・複勝（リアルタイム・確定） | JV-Data |
| 馬体重 | 当日馬体重、前走からの増減 | JV-Data |
| レース条件 | 天候、馬場状態、コース、距離、クラス | JV-Data |
| 手動パドック評価 | 毛艶・発汗・歩くリズム（状態系）、脚の長さ・胴の長さ・トモの筋肉量・胸前の筋肉（馬体系） | 手動入力（M4） |

### 6.2 出力データ項目

| カテゴリ | 主な項目 |
|---|---|
| 予想結果 | 出走馬ごとの統合スコア、ランキング |
| 買い目候補 | 単勝・複勝・馬連の推奨銘柄（馬番） |
| ファクター内訳 | 各馬のスコアに対するファクター別の寄与度 |
| バックテスト結果 | 的中率、回収率（ROI）、対象レース数 |
| 重み探索結果 | 重み候補ごとの的中率・回収率ランキング |

## 7. 非機能要件

| 区分 | 内容 |
|---|---|
| 実行環境 | JV-Link（ActiveX/COM）はWindows専用・サーバー非対応のため、Windows PC上で動作するローカルスクリプトとして実装する。 |
| 想定利用者 | 個人（1名）による非公開・非商用利用。 |
| 性能目安 | 1開催（1日）分のデータ取得〜スコアリングが数分以内に完了すること。 |
| データ保存 | ローカルDB（SQLite）にレース結果・特徴量・予測ログを蓄積する。 |
| セキュリティ | JRA-VAN利用キー等の認証情報は環境変数または設定ファイル（バージョン管理対象外）で管理する。 |

## 8. 制約・リスクと対応方針

| 制約・リスク | 対応方針 |
|---|---|
| JV-LinkがWindows専用・サーバー非対応 | Windows環境での運用を前提とし、サーバー化は将来課題とする。 |
| JRA-VANデータの二次利用・商用利用の規約制限 | 個人利用に限定し、外部への公開・販売・再配布・実データのコミットは行わない。 |
| JRA-VAN契約・JV-Link利用キーが未取得 | 実データ接続部分はインターフェースを分離して設計し、モックデータで他のロジックの開発・検証を進める。契約完了後に接続部分のみ実装する。 |
| 実過去データ不足によるバックテストの限界 | 実データ入手までは合成データでパイプラインの動作確認のみを行い、算出される回収率等の数値をもって精度を判断しない。 |
| 手動パドック評価の採点基準が未定義 | 採点者（利用者）ごとの一貫性を担保するため、採点基準の具体化（12.1）を運用開始前に完了させる。 |

## 9. 評価指標

- 的中率（券種別: 単勝・複勝）
- 回収率（ROI）
- 上位N頭内包含率（実際の上位入着馬がスコア上位N頭に含まれる割合）※Phase 1では未実装、必要性を含め要検討

## 10. 使用技術スタック

| 区分 | 技術 |
|---|---|
| 言語 | Python |
| データ取得 | JV-Link（Windows/ActiveX、pywin32でCOM呼び出し予定） |
| データ処理 | 標準ライブラリ中心（sqlite3等）。pandas等の導入は必要に応じて検討 |
| データベース | SQLite |
| UI | CLI／CSV出力（Streamlit導入は未定） |
| テスト | pytest |

## 11. 開発フェーズ計画

| フェーズ | 名称 | 内容 |
|---|---|---|
| Phase 1 | MVP構築（本書の対象） | 定量ファクターと手動パドック評価スコアを用いたスコアリングエンジン・バックテストの構築 |
| Phase 2 | パドックAI試験導入 | パドック動画による歩様特徴量抽出（M3）を試験導入し、採用可否を判断 |
| Phase 3 | モデル統合・継続改善 | 機械学習モデルへの移行、パドックAI特徴量を含めた重み最適化 |

## 12. 未決定事項・要確認事項

実装を再開する前に、以下を確認・決定する必要がある。

### 12.1 ブロッカー（実運用に必須）

| # | 項目 | 内容 |
|---|---|---|
| 1 | 過去レースデータの入手方法 | バックテスト（M8）・重み探索（M9）を意味のあるものにするには、実際の過去レース結果とオッズが必須。JV-Data経由か、別途CSV等で入手するかを決定する。 |
| 2 | JRA-VAN会員登録・DataLab契約 | M1を実データに接続するために必要（詳細は下記12.3）。 |
| 3 | 手動パドック評価の採点基準の具体化 | 「毛艶+2」「発汗-1」等、各項目の点数に対応する具体的な観察基準を定義する。採点者（利用者）が現場で一貫した基準で採点できるようにするため。 |

### 12.2 重要（精度検証に影響）

| # | 項目 | 内容 |
|---|---|---|
| 4 | M6重み付けの妥当性 | 現行の重み（5.4節）はユーザーヒアリングに基づく暫定値。実データによるバックテスト・重み探索（M9）で妥当性を検証し、必要に応じて調整する。 |
| 5 | 評価指標の優先順位 | 回収率と的中率のどちらを重視するか（M9の目的関数、9章の評価指標に影響）。 |
| 6 | DBがCLIパイプラインに未接続 | §7で「ローカルDBにレース結果・特徴量・予測ログを蓄積する」と定めているが、現状`db.py`はどのCLIサブコマンドからも呼び出されておらず、実行のたびに再取得・再計算するのみで永続化されていない。設計上のギャップとして`docs/01_アーキテクチャ設計.md`§5、`docs/05_処理フロー.md`UC5に詳細を記載。次回の実装対応で解消する。 |

### 12.3 参考: JRA-VAN関連で必要な情報（前回回答の再掲）

| # | 項目 | 内容 |
|---|---|---|
| a | JRA-VAN会員登録 | 基本会員登録（無料）。会員ID・パスワードが発行される。 |
| b | DataLab.契約 | 有料オプション（月額）。JV-Dataの取得権限を得るために必要。 |
| c | JV-Linkのインストール | JRA-VAN公式サイトから無料配布。初回起動時に会員ID・パスワードで認証。 |
| d | JV-Data仕様書 | JRA-VAN開発者向けページで公開されているレコード種別・フィールド定義。実装に必須。 |
| e | 動作確認環境 | Windows PC上でpywin32（win32com.client）が使用できる環境（現行の開発環境で対応可能）。 |

### 12.4 保留（Phase 2以降）

- パドック動画の合法的・現実的な入手方法（撮影可否・著作権の確認を含む）
- Phase 2完了時点でのパドックAI（M3）の採用可否判断基準

## 13. 承認

| 役割 | 氏名 | 承認日 |
|---|---|---|
| 発注者（利用者） | | |
| 実装担当（Claude） | Claude Sonnet 5 | |

## 14. 関連ドキュメント

本書に続く設計成果物一式は`docs/`フォルダに格納している。索引は[docs/00_README.md](./docs/00_README.md)を参照。

| ドキュメント | 内容 |
|---|---|
| [docs/01_アーキテクチャ設計.md](./docs/01_アーキテクチャ設計.md) | 構成図、コンポーネント図、デプロイメント図 |
| [docs/02_画面設計.md](./docs/02_画面設計.md) | 画面一覧、画面遷移、主要画面 |
| [docs/03_データ設計.md](./docs/03_データ設計.md) | ER図、テーブル定義 |
| [docs/04_IF一覧.md](./docs/04_IF一覧.md) | CLIサブコマンド一覧、モジュール間インターフェース一覧 |
| [docs/05_処理フロー.md](./docs/05_処理フロー.md) | 主要ユースケースのシーケンス図 |
| [docs/06_インフラ設計.md](./docs/06_インフラ設計.md) | 環境構成、ネットワーク構成、CI/CD |
| [docs/07_セキュリティ設計.md](./docs/07_セキュリティ設計.md) | 認証情報管理、脅威モデル、git衛生 |

---

本書の内容についてご確認いただき、修正点や決定事項があればお知らせください。承認後、本書に基づき実装を再開します。
