# 競馬予想支援ツール 仕様書（v0.1 ドラフト）

パドックAI解析 × 定量ファクター統合型（個人利用・非商用）

作成日: 2026-08-06

このファイルは本リポジトリで開発する「競馬予想支援ツール」の仕様書であり、実装作業を行う際の前提知識として参照すること。

## 1. 目的・背景

本ツールは、競馬の予想精度向上を目的として、パドックにおける馬体・気配・歩様の評価を中核ファクターの一つとして活用する予想支援ツールである。

事前調査の結果、パドック評価（気配・毛艶・歩様・馬体の張りなど）は経験や感覚に依存する定性的な要素が大きく、単純なルールで定量化することは難しいことが分かった。一方で、JRA関連企業（ギリア社等）が進める「AIパドックプロジェクト」のような先行事例では、パドック動画から姿勢推定（骨格検出）を行い歩様を特徴量化する取り組みが行われている。ただし個人開発者による同種の試み（note等で公開されている事例）では、以下のような課題が報告されている。

- カメラ角度や馬の向きにより解析結果のノイズが大きい
- 十分な精度を得るには相当量の学習データが必要（先行事例では約1,000頭規模）
- 現時点の個人レベルの実装では「精度は高いとは言えない」とする報告がある

以上を踏まえ、本ツールは次の方針で設計する。

- パドック動画のAI解析（姿勢推定による歩様特徴抽出）を採用するが、精度限界を前提に、オッズ・血統・スピード指数・馬体重変化・クラス・騎手・調教師データ等の定量ファクターと組み合わせたハイブリッド型スコアリングエンジンとする。
- 開発をフェーズ分けし、まず定量ファクターのみで予想・バックテストが機能するMVPを先に構築し、その後パドックAIモジュールを試験的に追加して精度を継続検証する（詳細は7章）。

## 2. 用語定義

| 用語 | 説明 |
|---|---|
| パドック | レース前に出走馬を周回させ、馬体・気配・歩様等を関係者や観客が確認するエリア。 |
| JV-Link / JV-Data | JRA-VANが提供するデータ取得用ソフトウェア（ActiveX/COM）およびそれを通じて取得できるレース関連データ。 |
| DataLab.（データラボ） | JRA-VANが提供する競馬データ会員サービス。月額課金制で、JV-Data等の基礎データを利用できる。 |
| 姿勢推定（Pose Estimation） | 画像・動画から人物や動物の関節・骨格キーポイントの座標を検出するAI技術（例: MediaPipe、YOLO-Pose）。 |
| 歩様解析 | 歩き方（歩幅・リズム・左右対称性等）を数値的に分析すること。 |
| スコアリングエンジン | 各ファクターの値を統合し、出走馬ごとの評価値（スコア）を算出する処理系。 |
| バックテスト | 過去のレース結果に予想ロジックを適用し、的中率・回収率等の成績をシミュレーションする検証手法。 |
| 回収率（ROI） | 投資額に対する回収額の割合。100%を超えると期待値上プラスであることを示す。 |

## 3. システム全体構成

データは M1〜M4 で収集・入力され、M5 で統合、M6 でスコア化・予測、M7 で出力、M8 で継続的に精度を検証する構成とする。

| ID | モジュール名 | 役割概要 |
|---|---|---|
| M1 | 基礎データ収集モジュール | JV-Link/DataLab経由でレース番組表・出走馬情報・血統・オッズ・馬体重・過去成績・天候・馬場状態を取得しローカルDBへ保存する。 |
| M2 | パドック動画取得・前処理モジュール | 自前で収集したパドック動画を取り込み、フレーム抽出・馬ごとの切り出し（トラッキング）・画質補正を行う。 |
| M3 | 姿勢推定・歩様特徴抽出モジュール（AI） | M2の動画からキーポイント座標を抽出し、歩幅・リズム・左右対称性等の歩様特徴量を算出する。 |
| M4 | 手動パドック評価入力モジュール | AI特徴量が不十分な場合の代替・補完として、気配・毛艶・気合い・発汗等を目視でチェックリスト採点し入力する。 |
| M5 | 特徴量統合・前処理モジュール | M1・M3・M4の出力を統合し、欠損値処理・正規化・特徴量エンジニアリングを行う。 |
| M6 | スコアリング・予測モデルモジュール | 初期はルールベーススコア、中期以降は機械学習モデル（LightGBM等）で出走馬ごとの評価値・推定確率を算出する。 |
| M7 | 出力・UIモジュール | ランキング表示、買い目候補（単勝・複勝・馬連等）の提示、ファクター別の評価根拠を表示する。 |
| M8 | バックテスト・検証モジュール | 過去データにモデルを適用し、的中率・回収率・ROIをシミュレーションし精度改善に利用する。 |

> 注意: JV-Link/DataLabはパドック動画そのものを提供しない。M2で用いる映像は別途、現地での自己撮影等により入手する想定である（8章のリスクを参照）。

## 4. 機能要件

### 4.1 M1 基礎データ収集モジュール

- 入力: JRA-VAN利用キー、対象レースID・開催日
- 処理: JV-Link経由でJV-Data（番組表・出走馬・血統・オッズ・馬体重・成績・天候／馬場状態）を取得し、ローカルDBに保存・更新する。
- 出力: レース単位・出走馬単位の基礎データテーブル
- 技術候補: JV-Link（Windows/ActiveX）、Python（pywin32等でCOM呼び出し）、SQLite/PostgreSQL

### 4.2 M2 パドック動画取得・前処理モジュール

- 入力: 現地等で撮影したパドック動画ファイル（馬番・レースIDと紐付け）
- 処理: 動画のフレーム分割、対象馬の検出・トラッキングによる個体別クリップ切り出し、明るさ・ブレ補正
- 出力: 馬番ごとの整形済み動画クリップ
- 技術候補: OpenCV、物体検出（YOLO系）による馬体検出・追跡

### 4.3 M3 姿勢推定・歩様特徴抽出モジュール（AI）

- 入力: M2で出力した個体別動画クリップ
- 処理: 脚・体幹等のキーポイント座標を時系列で抽出し、歩幅・歩行リズム・左右対称性・重心の揺れ等を数値化する。ノイズ除去（カメラ角度補正等）を行う。
- 出力: 馬ごとの歩様特徴量ベクトル
- 技術候補: MediaPipe Pose／YOLOv8-Pose等の姿勢推定モデル、時系列処理（統計量集約またはTransformer等）
- 留意点: 先行事例でも精度・ノイズの課題が報告されており、本モジュールは7章のフェーズ2で試験導入とし、精度を継続検証する。

### 4.4 M4 手動パドック評価入力モジュール

- 入力: 利用者本人の目視によるパドック観察結果
- 処理: 気配・毛艶・気合い（ハリ）・発汗・落ち着き・歩くリズム等（状態系）、および脚の長さ・胴の長さ・トモの筋肉量・胸前の筋肉等（馬体系）をあらかじめ定義した採点基準（加点／減点方式）でチェックリスト入力し、馬ごとの評価スコアに変換する。
- 出力: 馬ごとの手動パドック評価スコア
- 技術候補: 簡易入力UI（Streamlit／Excelフォーム等）

### 4.5 M5 特徴量統合・前処理モジュール

- 入力: M1（基礎データ）、M3（AI歩様特徴量）、M4（手動評価スコア）
- 処理: 各データソースをレース・馬番単位で結合し、欠損値処理・正規化・派生特徴量（例: 前走からの馬体重変化率、クラス昇降、騎手の当該距離・馬場での成績等）を生成する。
- 出力: 統合特徴量テーブル
- 技術候補: pandas

### 4.6 M6 スコアリング・予測モデルモジュール

- 入力: M5の統合特徴量テーブル
- 処理: 初期はファクターごとの重み付け加算によるルールベーススコアを採用。ファクターはJV-Dataで裏付けられる定量指標（オッズ、馬体重、騎手・調教師成績、過去成績由来のクラス昇降・近走成績〈スピード指数近似〉）を主軸に、M4の手動パドック評価を組み合わせる。血統は父・母・母父の名称のみでは強さを定量化できないため、外部の産駒成績データベースを別途組み込むまでは対象外とする。データ蓄積後は機械学習モデル（勝敗・着順を目的変数とした分類／回帰）へ段階的に移行する。
- 出力: 出走馬ごとの評価スコア・推定勝率（または推定確率）
- 技術候補: scikit-learn、LightGBM、XGBoost

### 4.7 M7 出力・UIモジュール

- 入力: M6のスコア・推定確率
- 処理: 出走馬をスコア順にランキング表示し、券種別（単勝・複勝・馬連等）の買い目候補を提示する。各馬のスコアがどのファクターに基づくかを内訳表示する。
- 出力: ランキング画面、買い目候補リスト、ファクター別内訳
- 技術候補: Streamlit、またはExcel／CSV出力

### 4.8 M8 バックテスト・検証モジュール

- 入力: 過去レースの統合特徴量・実際の結果・確定オッズ
- 処理: モデル／ルールを過去データに適用し、券種別に的中率・回収率（ROI）・上位N頭内包含率等を集計する。
- 出力: バックテスト結果レポート（的中率・回収率・期間別成績等）
- 技術候補: 自作シミュレーションスクリプト（Python）

## 5. 非機能要件

| 区分 | 内容 |
|---|---|
| 実行環境 | JV-Link（ActiveX/COM）はWindows専用でサーバー非対応のため、本ツールはWindows PC上で動作するデスクトップアプリ／ローカルスクリプトとして実装する。将来的な分離構成（データ取得はWindows、分析はLinux等）も検討可能とする。 |
| 想定利用者 | 個人（1名）による非公開・非商用利用を前提とする。 |
| 性能目安 | 1開催（1日）分のデータ取得〜スコアリングが数分以内に完了することを目安とする。 |
| データ保存 | ローカルDB（SQLite等）にレース結果・特徴量・予測ログを蓄積し、モデル改善・バックテストに利用する。 |
| 拡張性 | パドックAIモジュール（M3）の精度向上に応じて重み付けを調整できるよう、特徴量抽出とスコアリングロジックを分離して設計する。 |
| セキュリティ | JRA-VAN利用キー等の認証情報は環境変数または設定ファイル（バージョン管理対象外）で管理する。 |

## 6. データ仕様

### 6.1 入力データ項目

| カテゴリ | 主な項目 |
|---|---|
| 出走馬情報 | 馬名、年齢、性別、斤量、騎手、調教師、血統（父・母・母父）、所属 |
| 過去成績 | 過去レースの着順、タイム、上がり3ハロン、通過順位、クラス、距離・馬場適性 |
| オッズ | 単勝・複勝・馬連・ワイド等（リアルタイム・確定） |
| 馬体重 | 当日馬体重、前走からの増減 |
| レース条件 | 天候、馬場状態（良・稍重・重・不良）、コース、距離、クラス |
| 手動パドック評価 | 気配、毛艶、気合い、発汗、落ち着き、歩くリズム（状態系）、脚の長さ、胴の長さ、トモの筋肉量、胸前の筋肉（馬体系）等の採点結果（M4） |
| パドック動画 | 自前収集の映像（M2／M3で使用、JV-Linkからは取得不可） |

### 6.2 出力データ項目

| カテゴリ | 主な項目 |
|---|---|
| 予想結果 | 出走馬ごとの統合スコア／推定勝率、ランキング |
| 買い目候補 | 券種別の推奨買い目候補 |
| ファクター内訳 | 各馬のスコアに対するファクター別の寄与度 |
| バックテスト結果 | 的中率、回収率（ROI）、上位N頭内包含率、期間別成績 |

## 7. 開発フェーズ計画

| フェーズ | 名称 | 内容 |
|---|---|---|
| Phase 1 | MVP構築 | 定量ファクター（オッズ・血統・スピード指数・馬体重変化・クラス等）と手動パドック評価スコア（M4）を用いてスコアリングエンジン（M6）とバックテスト（M8）を構築し、実運用検証を開始する。 |
| Phase 2 | パドックAI試験導入 | 自前収集のパドック動画を用いて姿勢推定による歩様特徴量抽出（M3）を試験導入する。手動評価スコアとの相関、および単独での予測寄与度を検証し、採用可否を判断する。 |
| Phase 3 | モデル統合・継続改善 | 十分なデータ（数百〜数千頭規模を目安）が蓄積された段階で機械学習モデルへ移行し、パドックAI特徴量を含めた重み最適化と継続的な精度検証を行う。 |

**実装に着手する際は必ずPhase 1から始めること。** M2/M3（パドックAI）はPhase 2以降の試験導入扱いであり、Phase 1のスコープには含めない。

## 8. 制約・リスクと対応方針

| 制約・リスク | 対応方針 |
|---|---|
| JV-LinkがWindows専用・サーバー非対応 | Windows環境（デスクトップPC）での運用を前提に設計し、サーバー化は将来課題とする。 |
| JRA-VANデータの二次利用・商用利用の規約制限 | 個人利用に限定し、外部への公開・販売・再配布は行わない。 |
| パドック動画の入手手段が未確立（DataLabは動画非提供） | 現地での自己撮影・撮影可否の確認を行う。入手できない期間・レースは手動評価スコア（M4）のみで運用を継続する。 |
| パドックAI（M3）の予測精度が不十分な可能性 | 先行事例でも精度課題が報告されているため、Phase 2は「試験導入」に位置づけ、精度検証がクリアできない場合はM3を無効化しM4主軸で運用する。 |
| データ量不足によるモデル過学習 | 十分なサンプル数が蓄積されるまではルールベーススコア（Phase 1）を主軸とし、機械学習移行は慎重に判断する。 |

## 9. 使用技術スタック候補

| 区分 | 候補技術 |
|---|---|
| 言語 | Python |
| データ取得 | JV-Link（Windows/ActiveX、pywin32等でCOM呼び出し） |
| 動画処理 | OpenCV |
| 姿勢推定 | MediaPipe Pose、YOLOv8-Pose 等 |
| データ処理 | pandas、NumPy |
| 機械学習 | scikit-learn、LightGBM、XGBoost |
| データベース | SQLite（初期）／PostgreSQL（将来拡張時） |
| UI | Streamlit、または Excel／CSV出力 |
| バックテスト | 自作シミュレーションスクリプト（配当計算ロジックを含む） |

## 10. 評価指標

- 的中率（券種別: 単勝・複勝・馬連・ワイド等）
- 回収率（ROI）
- 上位N頭内包含率（実際の上位入着馬がスコア上位N頭に含まれる割合）
- 機械学習モデル導入後は AUC／LogLoss 等のモデル評価指標も併用する

## 11. 今後の検討事項（オープン課題）

- パドック動画の合法的・現実的な入手方法の確定（撮影可否・著作権の確認を含む）
- 手動パドック評価UIの採点基準表（加点・減点項目の定義）の作成
- JRA-VAN利用キーの取得・DataLab契約手続き
- 週末のレース開催に合わせた運用フロー（データ取得〜予想〜検証）の確立
- Phase 2完了時点でのパドックAI（M3）の採用可否判断基準の具体化

## コーディング時の指針（Claude向け）

- 実装を依頼された場合は、まずPhase 1（M1・M4・M5・M6・M7・M8）の範囲で構築すること。M2/M3（パドック動画AI解析）はユーザーから明示的に指示がない限り着手しない。
- JV-LinkはWindows専用・ActiveX/COMのため、Linux環境で動作確認できない処理（データ取得部分）はモック化し、それ以外のロジック（スコアリング・バックテスト等）は環境非依存で実装・テストできるようにすること。
- JRA-VANデータの二次配布・商用利用は規約上不可のため、生成するコードやドキュメントに実データをコミットしない。
